import { Lato, Syncopate } from 'next/font/google'

export const lato = Lato({
  subsets: ['latin'],
  weight: ['400', '700', '900',],
  display: 'swap',
})

export const syncopate = Syncopate({
  subsets: ['latin',],
  weight: ['400', '700',],
  display: 'swap',
})
